# 项目更新日志 - 2025-11-28

## 1. 问题诊断与分析

在深入分析 `validation_report.md` 和模型表现后，我们识别出了阻碍预测精度提升的两个关键核心问题：

### A. 响应滞后 (The Lag Mismatch)
*   **现象**: 模型对气候突变（如2025年5月的降温）的反应滞后约 2-3 个月。
*   **原因**: 原模型使用**当月**的 ENSO 指数预测**当月**的气温。然而，物理上太平洋海温变化（ENSO）传导到全球大气温度存在热惯性。
*   **验证**: 通过 `analyze_lags.py` 分析，我们确认了 **3个月的滞后 (Lag-3)** 与气温异常的相关性最强 (Pearson Corr: 0.147 vs Lag-0: 0.120)。

### B. 数据缩放严重错误 (Scaling Critical Bug)
*   **现象**: 在调试过程中发现，模型预测值和实际值在数值上偏差巨大（预测值 ~100，实际值 ~1.2）。
*   **原因**: GISTEMP 源文件 (`GLB.Ts+dSST.txt`) 的单位是 **0.01°C** (例如 "137" 代表 1.37°C)，但原本的 `data_handler.py` 直接读取了整数而未进行 `/100` 处理。
*   **影响**: 这导致之前的 RMSE 和 MSE 计算数值虽然在相对趋势上有效，但在绝对物理量级上是错误的。

## 2. 执行的改进措施

### 2.1 特征工程：引入滞后变量
我们修改了核心预测逻辑，不再使用原本的 `ENSO_ANOM`，而是构建了新的特征 `ENSO_ANOM_LAGGED`：
*   **文件**: `main_robust.py`, `validate_robust_methods.py`
*   **改动**: 
    *   在数据预处理阶段，将 ENSO 数据向后平移 **3个月**。
    *   模型现在使用 3个月前的 ENSO 信号来预测当月气温，这符合物理传导规律，理论上能消除滞后误差。

### 2.2 修复数据加载器
*   **文件**: `data_handler.py`
*   **改动**: 在 `get_clean_data` 函数中增加了 `/ 100` 的缩放操作。
    ```python
    df_long['Anomaly'] = pd.to_numeric(df_long['Anomaly'], errors='coerce') / 100
    ```

### 2.3 代码重构
*   更新了 `main_robust.py` 的接口，使其接受 `optimal_enso_lag` 参数，增加了代码的灵活性。
*   更新了验证脚本 `validate_robust_methods.py` 以匹配新的特征工程逻辑。

## 3. 下一步计划

由于修复了数据缩放错误，接下来的回测结果将反映真实的物理误差（摄氏度）。

1.  **执行新一轮回测**: 运行修复后的 `validate_robust_methods.py`，验证 Lag-3 策略是否成功消除了 2025年5月-8月的滞后现象。
2.  **目标评估**: 检查新的 MAE 是否能进入 **±0.05°C** 的理想区间。
3.  **生成最终预测**: 如果验证通过，将运行 `main_robust.py` 生成 2025年11月及未来的最终预测报告。

*(2026.02 更新：以上验证均已通过。为防止与早期基线混淆，所有非“稳健版”的配置及废弃文件均已从仓库中移除。后续所有预测与迭代将严格基于 `main_robust.py` 的架构。)*
