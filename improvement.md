📋 改进计划
基于风险评估结果，我制定了以下渐进式改进计划，从最关键到次要依次实施：

🎯 方案1：混合评估策略（推荐优先实施）
目标
解决样本量不足问题，同时保留季节性优势

具体实现
混合RMSE = α × RMSE(同月) + (1-α) × RMSE(全局)

其中：
- α = 季节性权重，建议值 0.6-0.7
- 当同月样本数 ≥ 3 时，α = 0.7
- 当同月样本数 = 2 时，α = 0.5
- 当同月样本数 = 1 时，α = 0.3（降低季节性权重）

Copy
优势
✅ 平衡了季节性针对性和统计可靠性

✅ 样本少时自动降低季节性权重

✅ 实现简单，风险可控

配置参数
# config.py 新增
SEASONAL_WEIGHT_BASE = 0.7  # 基础季节性权重
SEASONAL_WEIGHT_MIN_SAMPLES = 3  # 最小样本数阈值

Copy
python
🎯 方案2：近期趋势调整因子
目标
解决对近期突变反应不足的问题

具体实现
最终预测 = 集成预测 + 趋势调整

趋势调整计算：
1. 计算最近6个月的线性趋势斜率 (slope_6m)
2. 计算最近12个月的线性趋势斜率 (slope_12m)
3. 如果 |slope_6m| > |slope_12m| × 1.5：
   趋势调整 = slope_6m × 未来月数 × 调整权重
   调整权重 = 0.15（可配置）

Copy
触发条件
仅在检测到趋势加速时启用

避免过度调整

配置参数
# config.py 新增
TREND_ADJUSTMENT_WEIGHT = 0.15  # 趋势调整权重
TREND_ACCELERATION_THRESHOLD = 1.5  # 加速阈值

Copy
python
🎯 方案3：扩展窗口范围
目标
增加同月样本数量

具体实现
当前窗口：12-36月，每3月一个（9个窗口）
扩展窗口：9-48月，每3月一个（14个窗口）

新增窗口：-9月, -12月, -39月, -42月, -45月, -48月

Copy
效果预估
-39到-48月窗口可提供3个同月样本

但训练数据更旧，可能降低准确性

权衡
✅ 增加样本量

⚠️ 训练数据时效性降低

建议：仅在样本数<2时启用长窗口

🎯 方案4：Bootstrap置信区间（可选）
目标
评估RMSE的统计可靠性

具体实现
对每个窗口：
1. 对同月样本进行有放回抽样（1000次）
2. 计算每次的RMSE
3. 得到RMSE的95%置信区间
4. 如果置信区间过宽（CV > 0.5），降低该窗口权重

Copy
用途
识别"偶然表现好"的模型

提供预测可靠性指标

复杂度
中等（需要额外计算）

建议作为可选功能


📊 推荐实施顺序
阶段1：核心改进（必须）
✅ 方案1：混合评估策略

优先级：⭐⭐⭐⭐⭐

工作量：小

效果：直接解决样本量问题

✅ 方案2：近期趋势调整

优先级：⭐⭐⭐⭐

工作量：小

效果：提升对突变的敏感度

阶段2：增强改进（建议）
⭐ 方案3：扩展窗口范围

优先级：⭐⭐⭐

工作量：极小（只需修改配置）

效果：增加样本量，但有副作用

阶段3：高级功能（可选）
⭐ 方案4：Bootstrap验证

优先级：⭐⭐

工作量：中等

效果：提供可靠性评估

🔧 配置文件扩展计划
# config.py 新增配置项

# ========== 混合评估策略 ==========
USE_HYBRID_EVALUATION = True  # 是否使用混合评估
SEASONAL_WEIGHT_BASE = 0.7  # 基础季节性权重（样本充足时）
SEASONAL_WEIGHT_DECAY = {  # 样本不足时的权重衰减
    1: 0.3,  # 1个样本：权重0.3
    2: 0.5,  # 2个样本：权重0.5
    3: 0.7,  # 3+个样本：权重0.7
}

# ========== 近期趋势调整 ==========
USE_TREND_ADJUSTMENT = True  # 是否启用趋势调整
TREND_ADJUSTMENT_WEIGHT = 0.15  # 趋势调整权重
TREND_ACCELERATION_THRESHOLD = 1.5  # 趋势加速阈值
TREND_LOOKBACK_MONTHS = [6, 12]  # 趋势计算窗口

# ========== 窗口范围 ==========
RACE_SPLIT_MONTHS = list(range(12, 49, 3))  # 12-48月，每3月
USE_LONG_WINDOWS_ONLY_IF_NEEDED = True  # 仅在样本不足时使用长窗口

# ========== Bootstrap验证（可选）==========
USE_BOOTSTRAP_VALIDATION = False  # 是否使用Bootstrap
BOOTSTRAP_ITERATIONS = 1000  # Bootstrap迭代次数
BOOTSTRAP_CV_THRESHOLD = 0.5  # 变异系数阈值


Copy
📈 预期效果
改进方案	解决的问题	预期改进
混合评估	样本量不足	风险从5/5降至2/5
趋势调整	近期突变敏感度	风险从5/5降至3/5
扩展窗口	样本量不足	增加1-2个样本
Bootstrap	可靠性评估	提供置信度指标
综合效果：风险评分从5/5降至1-2/5

⚠️ 注意事项
不要过度优化：保持模型简洁性

保留原版本：作为对照组

逐步验证：每个改进单独测试效果

文档记录：记录每次改进的效果